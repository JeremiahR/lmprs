[package]
name = "lmp"
version = "0.1.0"
edition = "2021"

[dependencies]
lightning = "0.1.1"
secp256k1 = { version = "0.30.0", features = ["rand", "lowmemory"] }
tokio = { version = "1.43.0", features = [
    "net",
    "rt",
    "rt-multi-thread",
    "macros",
] }
